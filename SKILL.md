---
name: wechat-chat-customer-extract
description: 把 WeFlow 导出的微信聊天记录 xlsx（私聊_*.xlsx / 群聊_*.xlsx）解析成「客户联系方式 / 地区 / 购买设备」台账，写入 Excel 收集表。当用户提供聊天导出文件并要求「把聊天数据填入表格」「统计客户电话/微信号」「整理客户信息表」时使用；也适用于从聊天记录批量提取手机号、微信号、收件人、收货地址、发货设备。
agent_created: true
---

# 微信聊天记录 → 客户信息台账

## 何时用

- 用户给了 `私聊_XXX.xlsx` / `群聊_XXX.xlsx`（WeFlow 或同类工具导出的微信聊天）
- 目标：抽出客户联系方式（手机号 / 微信号）、地区、购买设备，写进一张 Excel 台账

## 目录结构

```
wechat-chat-customer-extract/
├── SKILL.md                       # 本文件：工作流与硬性约束
├── references/
│   ├── field-spec.md              # 台账字段规格（10 列）与各地区/异常值口径
│   ├── number-formats.md          # 号码变体全表 + 判定顺序（实测穷举）
│   └── group-chat-orders.md       # 群聊发货记录专项处理
└── scripts/
    ├── build_table.py             # 参数化生成空白台账模板（含公式/条件格式）
    └── parse_messages.py          # 可导入的解析工具（读聊天、号码归一化、分类、去重）
```

## 工作流

### Step 0 — 先确认环境与既有产物

1. **列一下工作区**，看用户已有的台账文件还在不在。
   ⚠️ 实测高频陷阱：**上一轮被编辑器打开的那个 xlsx，下一轮往往已不在磁盘上**。
   若采用「脚本生成」路线，开工前先跑一次重建脚本把文件补回来。
2. 用户如果已经有台账，**必须先读它的表头和 sheet 结构**再动手，不要自己另起一套字段。
3. 用户如果只给了聊天记录、没给台账，用 `scripts/build_table.py` 建一张（字段见 `references/field-spec.md`）。

### Step 1 — 摸清源文件结构

先 dump 前 20 行确认列结构，**不要凭假设写解析**。WeFlow 常见结构见本文件末尾。

### Step 2 — 分类型抽取

三类信息要分开处理，判据和手法完全不同：

| 类型 | 典型特征 | 手法 |
|---|---|---|
| 实名订单 / 转介绍 | 含「地址 + 电话 + 设备 + 价格」的长文本 | **人工逐条核对**，见 Step 3 |
| 纯号码消息 | 整条只有号码，可能带前缀后缀、全角、括号 | 正则批量抓 + 去重，规则见 `references/number-formats.md` |
| 藏在文字里的号码 | `修13800138001陈`、`联系我？13800138002` | 单独扫描（见下），备注写原文让用户确认 |

**必须额外扫一遍「含号码但不是纯号码」的消息**，否则一定漏号：

```python
import re
MOBILE = re.compile(r'1[3-9]\d{9}')
BARE = re.compile(r'^[vV]?\s*[\d\s]{10,16}[vV]?$')
# 关注：MOBILE.search(s) 成立 且 not BARE.match(s) 的消息
```

号码的归一化与分类规则（实测穷举了 15 种写法）见 **`references/number-formats.md`**，写解析前必读。

### Step 3 — 实名记录：人工校对，不要硬调正则

**这是本技能最重要的一条经验。**

正则抽姓名不可靠，实测把 `河北省唐山市滦南县…酷车工场王五13800138003` 抽成「场王五」、
`山西省…某汽修厂13800138004李四` 抽成「东汽修厂」；城市正则还会把省份吃掉
（`浙江省杭州市` → 城市「江省杭州市」）。

**正确做法（三段式）：**

1. 脚本粗抽 → 打印全部含号码的消息原文（带日期、发送者）
2. **人工逐条比对，写死成常量表** `D = [(手机号, 收件人, 省份, 城市, 设备, 日期, 备注), ...]`
3. 脚本只负责按常量表写入

**手机号本身（11 位正则）是可靠的，可以放心自动化**；不可靠的只是「姓名 / 设备 / 城市」这几个需要语义判断的字段。
40 条记录人工过一遍约十几分钟，质量远高于继续调正则。

### Step 4 — 写入

用 `scripts/build_table.py` 建模板，再写常量表。**写入时必守两条**：

- `ws.cell(row, col, value=None)` **不会清空**已有单元格（openpyxl 源码是 `if value is not None`）。
  重跑脚本时必须 **从干净模板重建**，或用 `ws.cell(row=r, column=c).value = v if v else None` 显式赋值。
  否则会出现行错位、号码莫名重复。
- 手机号 / 微信号列 `number_format = "@"`（否则 11 位号码变科学计数法）；
  日期写 `datetime.date` 对象，列格式 `yyyy-mm-dd`（否则无法排序）。

### Step 5 — 回读校验（不可省）

每次写完必须回读并打印：

- 行数
- **重复号码**（`collections.Counter`）
- 缺号码的行数
- 逐列填充数（哪列空了多少）
- 有多个表时，**跑一次跨表号码交叉比对**并报告重叠

任何一项异常都说明 Step 4 踩了坑，先修再交付。

## 交付与收尾

1. 「拿不准」的条目**全部落到备注列**，并在回复里单独列一遍给用户确认（位数异常、原文自相矛盾、收件人只有姓、省份没写等）
2. **报价 / 金额是否入表要先问用户**——用户说「只记设备名称」就不要擅自加金额列
3. 提醒用户同目录还有哪些导出没处理
4. 多份导出时：每份建一张表，文件名 `客户信息收集表_<来源>.xlsx`；
   在工作区留一个 `rebuild_all.py` 串起全部「建模板 → 填充」步骤，**保证产物可一键复现**

## 硬性约束速查

- 省份只填原文写出的，**用户选「不补」就不要按城市反推省份**（`西安市` → 省份留空）
- 直辖市：省份填市名，城市列留空（不要两列重复填同一个市名）
- 号码位数不对：照收 + 备注「待核对」，不丢弃、不猜
- 不被要求就不合并不同来源的表；跨表重叠只报告，等用户决定

## 源文件结构（WeFlow 0.0.2 参考，务必先验证）

单个 sheet `聊天记录`，前 4 行为会话头，**第 5 行起是消息**：

| 列 | 含义 |
|---|---|
| A | 序号 |
| B | 时间（`2026-08-25 15:27:18`） |
| C | 发送者身份（私聊里 `我` = 账号本人；群聊里是各自昵称） |
| D | 消息类型（文本 / 图片 / 转账 / 名片 / 引用 / 系统 / 通话…） |
| E | 内容 |

第 2 行含 `微信ID` / `昵称`，可用来确定聊天对象。

## 参考资料

- 字段规格与地区/异常值口径 → `references/field-spec.md`
- 号码变体与判定顺序（实测穷举）→ `references/number-formats.md`
- 群聊发货记录处理 → `references/group-chat-orders.md`
- 可直接导入的解析工具 → `scripts/parse_messages.py`
